[project]
name = "home-stack-tooling"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
  "ansible==13.5.0",
  "psycopg2-binary==2.9.11",
]

[dependency-groups]
dev = [
  "ansible-lint==26.4.0",
  "lefthook==2.1.6",
  "yamllint==1.38.0",
]

[tool.uv]
package = false
