[project]
name = "home-stack-tooling"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
  "ansible-core==2.20.5",
  "ansible-lint==26.4.0",
  "yamllint==1.38.0",
]

[tool.uv]
package = false
