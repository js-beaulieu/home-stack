domain: ""
acme_email: ""
# Stable tofu entry-point credential — set once.
# To rotate: (1) connect to the VPS and run:
#   docker exec -it home-stack-postgres-1 psql -U home_stack_admin \
#     -c "ALTER ROLE home_stack_admin PASSWORD 'newpassword'"
# (2) update this value, (3) task rotate.
pg_admin_password: ""
# Stable tofu entry-point credential — set once.
# To rotate: (1) change it via the Keycloak admin UI at auth.<domain>/admin,
# (2) update this value, (3) task rotate.
keycloak_admin_password: ""
keycloak_admin_allowed_ips: ""
google_identity_provider:
  enabled: false
  client_id: ""
  client_secret: ""
  hosted_domain: ""
